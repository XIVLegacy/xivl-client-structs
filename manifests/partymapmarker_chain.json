{
  "version": 1,
  "generated": "2026-05-24",
  "gameVersion": "1.23b",
  "status": "manifest_complete_writer_hunt_deferred",
  "scope": "The party-subsystem pass noted s2c 0x018D PartyMapMarker handler gates on `*(int*)(zone+0x4d8) != 0` (active map widget). Plan asked to find writers to zone+0x4d8 to explain 592x retail emission rate. PIVOT: writer hunt requires UI-subsystem investigation outside pcap-driven scope (zone+0x4d8 = map UI widget pointer lifecycle, set on map-open, cleared on map-close). This completes the receive-side chain by cataloging 2 missing helpers from the party-subsystem pass's chain enumeration (FUN_006C1570, FUN_00573F10) and documents the writer hunt as a candidate.",
  "method": {
    "step1": "Re-read the party-subsystem manifest case 0x018D: chain enumeration is FUN_004DC690 case body -> FUN_00575550 (BCS-Y-0580, S2cDispatcher_HighSwitch_Case18D_Composite) -> FUN_006C1570 (UNCATALOGED) -> FUN_00573F10 (UNCATALOGED) -> FUN_0055CF70 (BCS-Y-0890, PartyMapMarker_ApplyToWidget).",
    "step2": "Decomp FUN_006C1570 (197B, 3 callers): conditional dispatch helper. Branches on `*(param_1+0x20) == DAT_0130C778` sentinel match. If match: thin path with FUN_00573F10. If miss: stages local_1c via FUN_006C0E90 or FUN_006C1510 (per param_3 zero-check), then FUN_00573F10. Callers: FUN_00575550 (0x18D handler), FUN_006C2190 (0x18B SetGroupLayoutId handler), FUN_00578540 (other party-related dispatch).",
    "step3": "Decomp FUN_00573F10 (86B, 2 callers): thin SEH-wrapped forwarder. Calls FUN_00573970(param_2, param_3) and returns param_1. Callers: FUN_00575550 (direct), FUN_006C1570 (via the stage-then-fire path).",
    "step4": "Document the writer hunt limitation: zone+0x4d8 is set when the user opens the in-game map UI. Finding the writer requires walking the UI/menu subsystem (likely a widget-create event handler that registers the new map widget pointer in zone+0x4d8). FindOffsetWriters is broken for register+displacement instructions per memory; byte-pattern search for [REG+0x4D8]= encodings would yield false positives due to common displacement value. Defer to later work (UI-widget lifecycle investigation - sibling subsystem, lower priority than network-tier work)."
  },
  "key_findings": [
    "PartyMapMarker (s2c 0x018D, 592x retail emissions) chain is now end-to-end cataloged for the RECEIVE side: FUN_004DC690 case 0x18D body -> FUN_00575550 (BCS-Y-0580) -> FUN_006C1570 (this manifest) -> FUN_00573F10 (this manifest) -> FUN_0055CF70 (BCS-Y-0890 apply). The gate condition `zone+0x4d8 != 0` (active map widget) is the explanation for why the server emits at a high rate but the client only acts when the UI is open.",
    "FUN_006C1570 is SHARED across 3 party-related dispatch paths (3 callers): 0x18D PartyMapMarker handler, 0x18B SetGroupLayoutId handler (via FUN_006C2190), and FUN_00578540 (other dispatch). This makes it a CROSS-OPCODE party-state update gateway, not a 0x18D-specific helper. Per-opcode attribution should reference both BCS-Y-1032 (this) AND the per-opcode handler entries.",
    "FUN_00573F10 is a thin SEH-wrapped forwarder (86B) to FUN_00573970. Shared by FUN_00575550 (0x18D direct path) and FUN_006C1570 (via the staged path). Likely a marker-rendering 'fire' helper that triggers the UI widget update.",
    "Writer hunt for zone+0x4d8: DEFERRED. The 0x4d8 offset on zone holds a pointer to the active in-game map widget. It's set when the player opens the map UI (likely a widget-create event handler in the UI subsystem) and cleared when the map closes. Finding the writer requires UI/menu-subsystem investigation (a sibling subsystem to network-tier work). FindOffsetWriters is broken for register+displacement per memory; byte-pattern search has high false-positive rate. The 592x retail emission rate vs typical 1-10 per session is explained by the gate: 591 of those emissions are silently dropped because the gate is closed (no active map UI), and the 1-10 that get through correspond to actual UI updates while the player has the map open.",
    "Architectural finding (party-subsystem cross-cutting): FUN_006C1570 being shared across 0x18D + 0x18B + FUN_00578540 reveals a CROSS-CUTTING party-state update gateway. The PARTY subsystem has at least 3 entry points (Map markers, group layout, other) all converging on this single update helper. Discovery candidate: enumerate FUN_00578540's caller cluster to identify the third path's opcode."
  ],
  "deferred_writer_hunt": {
    "target": "Writes to zone+0x4d8 (active-map-widget pointer)",
    "hypothesis": "Set in a widget-create event handler when the player opens the map UI; cleared when closed",
    "method_blocked": "FindOffsetWriters broken for register+displacement (per memory feedback)",
    "alternative_methods": "(a) Byte-pattern search for [REG+0x4D8]= encodings - high false-positive rate; (b) Walk UI widget creation code from known UI-subsystem entry points; (c) live capture live capture observing map-open/close events - DEFERRED per project direction",
    "priority": "Low - UI lifecycle is sibling to network-tier work, doesn't impact catalog completeness for s2c attribution"
  },
  "proposed_bcs_y_entries": [
    {"name": "PartySubsystem_CrossOpcodeUpdateGateway_FUN_006C1570", "kind": "function", "address": "0x006C1570", "confidence": "confirmed", "summary": "197-byte CROSS-CUTTING party-state update gateway shared across 3 dispatch paths (3 callers): [[S2cDispatcher_HighSwitch_Case18D_Composite_FUN_00575550_0055CF70_00573F70]] (BCS-Y-0580, 0x18D PartyMapMarker), FUN_006C2190 (0x18B SetGroupLayoutId path), FUN_00578540 (other party dispatch - identity TBD). Conditional dispatch: if `*(param_1+0x20) == DAT_0130C778` sentinel: thin path via [[PartySubsystem_MarkerFireForwarder_FUN_00573F10]]. Else: stages local_1c via FUN_006C0E90 (param_3 zero case) or FUN_006C1510 (param_3 non-zero case), then forwards via FUN_00573F10. The DAT_0130C778 sentinel likely identifies a specific party-state class. This catalog of the party-subsystem pass's chain enumeration."},
    {"name": "PartySubsystem_MarkerFireForwarder_FUN_00573F10", "kind": "function", "address": "0x00573F10", "confidence": "confirmed", "summary": "86-byte thin SEH-wrapped forwarder. Calls FUN_00573970(param_2, param_3) and returns param_1. 2 callers: [[S2cDispatcher_HighSwitch_Case18D_Composite_FUN_00575550_0055CF70_00573F70]] (BCS-Y-0580, direct path for 0x18D) and [[PartySubsystem_CrossOpcodeUpdateGateway_FUN_006C1570]] (BCS-Y-1032, via staged path). Likely a marker-rendering 'fire' helper that triggers the UI widget update by deferring to FUN_00573970. This catalog completes the 0x18D PartyMapMarker receive chain."}
  ]
}
