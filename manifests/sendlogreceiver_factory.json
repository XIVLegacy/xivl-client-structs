{
  "version": 1,
  "generated": "2026-05-24",
  "gameVersion": "1.23b",
  "status": "manifest_complete",
  "scope": "Resolve the sendlogreceiver demux pass's open question: characterize FUN_00762460 - the second caller of FUN_0089F5B0 SendLogReceiver ctor (alongside FUN_00759FD0 slot 63 wrapper). The sendlogreceiver demux pass hypothesized this is an alternate dispatch entry for heap-allocated receiver instances. Decomp + caller analysis confirms factory pattern: alloc check + in-place ctor + register-via-FUN_007945A0.",
  "method": {
    "step1": "Decomp FUN_00762460: 187 bytes. Body: iVar1 = FUN_009d1b35(); if (iVar1 != 0) FUN_0089f5b0(param_1); FUN_007945a0(). Pattern is alloc-check + ctor + register.",
    "step2": "Caller analysis: FindReferencesToAddress on 0x00762460 found 0 instruction-level callers across 3174303 scanned instructions. Function is reached EXCLUSIVELY via function-pointer table dispatch (vtable or factory table).",
    "step3": "Decomp FUN_007945A0: 87 bytes; stores param_1 on stack then calls FUN_007238b0(&local) - generic register-pointer helper with ~500 callers across the binary. This is a universal element-registration utility (probably std::vector push_back or list insertion).",
    "step4": "Decomp FUN_009D1B35: 105 bytes (likely _malloc or _operator_new wrapper based on body shape and call pattern). Has 500+ callers binary-wide, confirming it's a base allocator/initializer used pervasively.",
    "step5": "Synthesize: FUN_00762460 is a FACTORY function for heap-allocated SendLogReceiver instances. Pattern matches a typed-factory table where the dispatcher calls into a name- or id-keyed table to get the factory, which allocates and constructs in place. The Receiver class registry (receiver_discovery.json) is the most likely consumer."
  },
  "key_findings": [
    "FUN_00762460 is a HEAP-ALLOCATION FACTORY for SendLogReceiver. Pattern: (1) allocation/precondition check via FUN_009D1B35, (2) in-place constructor invocation via FUN_0089F5B0 (BCS-Y-0249) only if check passes, (3) unconditional registration via FUN_007945A0. The factory IS the 'alternate dispatch entry' the sendlogreceiver demux pass hypothesized - distinct from the slot 63 vtable wrapper FUN_00759FD0 (BCS-Y-0252) which assumes a stack-or-pre-allocated receiver.",
    "Zero instruction-level callers for FUN_00762460 (verified via FindReferencesToAddress scan of 3174303 instructions). The function is exclusively reached via function-pointer table dispatch. This is consistent with the typed-factory-registry pattern: a name- or id-keyed lookup returns the factory function pointer, which the caller invokes.",
    "FUN_007945A0 is a high-traffic registration utility (~500 callers in the binary). It is NOT specific to receivers; rather, it's a generic 'register this pointer in a shared list' utility, similar to a std::vector push_back. The factory pattern (FUN_00762460 -> FUN_007945A0) leverages this generic helper to insert the newly-constructed SendLogReceiver into whatever registry the system uses for active receivers.",
    "FUN_009D1B35 is also high-traffic (500+ callers). Combined with the alloc-pattern body, this is almost certainly a wrapper for _malloc / _operator_new / similar heap-allocator. The result-check + conditional ctor pattern in FUN_00762460 reflects a 'allocation can fail; don't construct on null' contract.",
    "Architectural finding: SendLogReceiver supports BOTH stack/static-allocation path (via FUN_00759FD0 slot 63 vtable wrapper) AND heap-allocation path (via FUN_00762460 factory). The first is invoked by the receiver-class registry's slot-63 dispatch when a SendLogReceiver instance already exists at a known location; the second is invoked when a NEW SendLogReceiver needs to be heap-allocated on demand (likely for dynamic receiver lifecycle - e.g. registering a Lua-side custom log handler at runtime). This dual-instantiation pattern likely applies to other Receiver classes too.",
    "Closes the sendlogreceiver demux pass's open question: the 'alternate dispatch entry for SendLogReceiver heap-allocated path' is confirmed to be FUN_00762460. No upstream-caller chain enumeration needed - the factory is invoked indirectly via function-pointer table dispatch from a typed-registry lookup (concrete consumer not identifiable without live capture / dynamic instrumentation, but the architectural role is clear)."
  ],
  "proposed_bcs_y_entries": [
    {"name": "SendLogReceiver_HeapFactory_FUN_00762460", "kind": "function", "address": "0x00762460", "confidence": "confirmed", "summary": "187-byte heap-allocation factory for SendLogReceiver. Second caller of [[SendLogReceiver::ctor_FUN_0089F5B0]] (BCS-Y-0249) alongside SendLogReceiver_Slot63Trampoline_FUN_00759FD0 (BCS-Y-0252). Body: alloc check via FUN_009D1B35 -> if non-zero, in-place ctor via FUN_0089F5B0(param_1) -> register via FUN_007945A0. Zero instruction-level callers (FindReferencesToAddress scan of 3174303 instructions) - reached EXCLUSIVELY via function-pointer table dispatch. Resolves the sendlogreceiver demux pass's open question: confirmed as the 'alternate dispatch entry for SendLogReceiver heap-allocated path'. SendLogReceiver supports both stack-path (slot 63 wrapper) and heap-path (this factory) - dual-instantiation pattern likely shared with other Receiver classes."}
  ]
}
