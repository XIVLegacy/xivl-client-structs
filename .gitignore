# Agent / AI tooling
.claude/
.agents/
AGENTS.md
CLAUDE.md
docs/ai_agents/local/

# Local editor and OS noise
.DS_Store
Thumbs.db
*.log
*.tmp

# Ghidra local project state
*.gpr
*.rep
*.lock
*.bak
.ghidra/

# Generated or scratch output
artifacts/
out/
tmp/
temp/
build/

# Headless Ghidra decomp logs: reproducible scratch, and bodies must not be
# committed (see README, AGENTS). Regenerate on demand from the workplan.
tools/ghidra/logs/

# Raw binary-derived extraction: near-verbatim RTTI metadata read out of
# ffxivgame.exe. Regenerate locally with tools/extractors/pe against your
# own client install. Cited entries resolve as in-repo-unresolved, the
# same class as the tools/ghidra/logs decomp evidence above.
manifests/rtti_extraction_OUR.txt

# Local MCP config and Ghidra MCP extension material
.mcp.json
.ghidra-mcp/

# Python tooling
__pycache__/
*.pyc
