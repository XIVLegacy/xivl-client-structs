# Signatures

Keep byte signatures here only when they are source-backed and useful for
finding a client function, global, vtable, or struct relationship.

Use `??` for wildcard bytes. Store the associated address as a hex string in
`manifests/symbols.json` and update the IR contract in the same change.

This directory currently holds no byte signatures.
